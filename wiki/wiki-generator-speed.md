---
id: "wiki-generator-speed"
title: "生成器速度工程：生成代码仿真器生成期优化的前提验证与守恒门"
description: "生成代码仿真器生成期（generator）速度优化的完整纪律：阶段计时器先于前提（被默认为文本格式化的收尾阶段实测发射仅占 1.6%、真实成本是调度构建 + 藏在调试 JSON dump 背后的重复构建）、canon/identity 哈希按记录并行的恒等条件（order-free 内容哈希因排序而与构建序无关）、合并循环调度构建的数据结构与查询联合优化（std::set 关系集合换排序平铺向量、可达性/路径查询占 77% 让位于按合并代备忘录化）、quotient 图上双向搜索的不 sound 证伪（gP 不是 gS 在合并群上的精确逆 → 带证据回滚），以及生成器改动的三重确定性门禁（字节恒等模型 + 种子回放 canon 点 + 纯决策函数差分 harness）"
tags: ["generator-speed", "phase-timers", "premise-check", "content-hash", "parallel-hash", "schedule-construction", "memoization", "quotient-graph", "bidirectional-search", "determinism-gate", "byte-identity", "differential-harness", "codegen", "rtl-sim"]
keywords: ["generator speed", "phase timer", "premise check", "emission fraction", "hidden duplicate build", "debug dump cost", "order-free hash", "per-record parallel hashing", "sorted serialization", "canon hash", "identity hash", "std::set to sorted vector", "flat vector adjacency", "reachability query", "path query", "merge-generation memoization", "bidirectional search unsound", "quotient graph invariant", "gP reverse of gS", "revert with evidence", "byte-identical model", "seed replay", "canon point", "differential harness", "pure decision function"]
last_updated: "2026-08-23"
---

# 生成器速度工程：前提验证与守恒门

## 概述

生成代码仿真器上，"生成一次"坐在每一次实验的关键路径上：A/B 两侧各是一次完整生成 + 编译（分钟到小时级），调参扫描、种子重录、回归门禁全部按生成次数付费。生成器变慢直接收缩每天可做的实验数，所以生成期优化（generator-speed engineering）本身就是一类值得系统对待的工程。

但它有两条普通性能工程没有的约束：

1. **输出必须守恒**：任何加速不得改变生成的模型内容——否则全部历史基线作废、所有种子失效（见 [[wiki-pointer-order-nondeterminism]] §9"种子与生成器同生共死"）。速度收益与输出恒等必须在**同一次改动**里交付，不存在"先快后对"的过渡态。
2. **前提必须实测**：生成器内部的成本结构（哪个 pass 占多少）没有可靠直觉。案例中一个被普遍默认为"文本格式化"的收尾阶段，装上阶段计时器后证明发射本身只占该阶段的 1.6%，真实成本是调度构建，外加一个藏在调试 JSON dump 背后的重复构建。

本文按一次真实生成期优化战役的完整链条组织：前提检查（§1）、canon 哈希并行化（§2）、合并循环调度构建（§3）、生成器改动的确定性门禁（§4）、检查清单（§5）。与 [[wiki-value-change-census]] 的关系：那是"census before emit"用在**运行时生成的代码**上，本文是同一条纪律用在**生成器自身**上——先数清成本，再动手。

---

## 1. 前提检查：阶段计时器先于优化

### 1.1 事故形态：一个从未被测量的"格式化"阶段

案例：一次完整生成耗时 44 分钟，收尾的"Final"阶段被团队默认为文本发射/格式化成本——这个前提从未被测量，却已经在指导优化讨论（"优化发射器/减少字符串拷贝"）。装上按阶段的墙钟计时器后：

| 前提 | 实测 |
|---|---|
| Final ≈ 发射/格式化 | **发射仅占 Final 的 1.6%** |
| （未被讨论） | 真实主体是**调度构建**（合并循环，见 §3） |
| （完全不可见） | 调试 JSON dump 背后藏着一次**重复构建**——dump 路径为了输出元数据，把已经建好的图结构重新构建了一遍 |

三个可迁移的判定点：

1. **未插桩区域 = 未知，而不是"琐碎"**。一个阶段没被计时，它就是总时长减去其它阶段的余项——把余项贴上"格式化"标签是命名，不是测量。
2. **调试/dump 路径是一等成本中心**。dump 为了输出而**重建**状态，等于把构建成本付两遍；dump 必须只读既有结构，或者用开关彻底门控。
3. **阶段名必须对应成本中心，而不是代码地理位置**。"Final" 这种按代码布局命名的阶段会吞掉一切没有单独插桩的工作。

### 1.2 纪律

**任何"瓶颈是 X"的前提，先装粗粒度按 pass 墙钟计时器再谈优化。**这件事零风险（不改变行为）、成本低（每个顶层 pass 一对时间戳），而且答案经常推翻共识——案例中前提（发射为主）与实测（发射 1.6%）之间差了两个数量级。这与 [[wiki-value-change-census]] §1 的"反事实不可推断"同构：比例（成本分布）是管线的联合属性，不能从代码外观推断，只能到真实执行的阶段上去数。

> 顺带的教训：**计时器本身按阶段输出，就顺带成为后续优化的回归基线**——§3 的每一次改动都能立即看到"Final 占比"是否真的下降了。

---

## 2. canon/identity 哈希的按记录并行

### 2.1 机制：order-free 是并行化的恒等条件

生成器的 canon/identity 哈希（对图状态做 order-free 内容哈希，用作确定性门禁与指纹，见 [[wiki-pointer-order-nondeterminism]] §4.2/§4.3）天然可并行，**且输出与串行版逐位相同**。原因是构造性的：

- 哈希定义在**排序后的记录序列**上：每条记录（成员名 + 排序端点名，排除 id/地址等不稳定字段）先做独立的逐记录摘要，再按**内容排序**的顺序折叠成总哈希。
- **排序使构建顺序无关**：总哈希只依赖记录内容集合，不依赖记录的插入/构建顺序。既然串行版的输出本来就不含顺序信息，把逐记录摘要分给多线程、再在规范序上合并，结果**必然**逐位相同——分片方式、线程数、调度时序都不进入输出。

### 2.2 与"顺序敏感但当前确定"的本质区别

并行化一个函数前必须先分类：

| 类别 | 判据 | 可否无门并行 |
|---|---|---|
| **order-free（按定义）** | 输出在数学上与处理顺序无关（排序后折叠、交换律合并） | **可以**——恒等是定理不是假设（本节案例） |
| 顺序敏感、但当前确定 | 输出依赖处理顺序，当前靠单线程/稳定容器保持一致 | 不可以——并行会改变输出，需要 §4 门禁 + 明确的顺序设计 |

把第二类误当第一类是生成器并行化最常见的静默事故：单线程下"恰好确定"掩盖了顺序依赖，多线程一开，输出开始漂移，而漂移要到字节比对/canon 门才被发现——离引入点已经隔了整条管线。

### 2.3 附带收益：并行化不弱化门禁，反而被门禁自检

canon 哈希本身就是确定性门禁（[[wiki-pointer-order-nondeterminism]] §4.3 的规范化指纹二分用的正是它）。并行化它不会弱化门禁：哈希的 order-free 定义不变，任何实现错误（比如合并时引入了顺序）都会让 canon 点在种子回放中立刻分叉——**门禁守护的函数自己也被门禁守护**。工程要点：

- 逐记录摘要的字段排除表（地址、分配序 id、指针值）必须与既有 canon 二分器完全一致，否则新旧哈希不可比；
- 合并用规范序折叠（或交换律算子），但**不要**同时改字段集——一次改动只动并行化，字段语义冻结，新旧实现才能在同图上对拍。

---

## 3. 合并循环调度构建：数据结构与查询的联合优化

§1 的计时器把"Final"的主体定位到调度构建（合并循环）。这一节的两个落地优化和一个被证伪的捷径。

### 3.1 数据结构：`std::set` 关系集合 → 排序平铺向量

合并循环把关系集合（邻接/依赖集）放在 `std::set` 里：每次成员测试付红黑树指针跳转 + 每个节点一次堆分配，缓存局部性差。换成**排序平铺向量**（一次构建、排序、之后二分/归并），去掉逐节点分配并恢复顺序扫描局部性。

与 [[wiki-pointer-order-nondeterminism]] §2 的衔接：`std::set<T*>` 的默认迭代序是指针序——排序平铺向量**顺带消除了这个非确定性隐患**，前提是排序键必须是内容键（成员名/行号类），而不是地址。一个性能改动同时是确定性修复，两个动机都写进 commit，避免后人"优化"回 `std::set` 时只看到性能理由。

### 3.2 查询：可达性/路径查询占 77% → 按合并代备忘录化

计时器进一步显示调度构建内部：**可达性/路径查询占整个生成的 77%**——合并循环的每一步都要在当前 quotient 图上问"X 可达 Y 吗"。优化不是换更快的搜索，而是**消掉重复提问**：

- quotient 图只在合并时变化。以**合并代**（merge generation）为缓存键：同一代内所有可达性结果不变，跨代作废重算。
- 缓存键必须枚举结果依赖的一切（图代数），这正是缓存正确性的全部条件——见 §5 检查清单第 7 条。

备忘录化把查询成本从"每次提问都遍历"压到"每代首次提问遍历"，而且**答案不变**（同一代上图相同 ⇒ 同一问题同一答案），因此它像 §2 一样是恒等保持的优化，只需 §4 的门禁确认实现无 bug。

### 3.3 被证伪的捷径：quotient 图上的双向搜索

可达性占 77% 时，双向搜索（从源正向、从汇反向，相遇即停）看起来是教科书式加速：搜索深度减半，期望平方级提速。**它在 live quotient 图上不 sound**：

- 双向搜索的正确性前提是：反向遍历用的前驱关系 gP 是正向后继关系 gS 的**精确逆**——每条正向边都有对应的显式反向边，粒度一致。
- 合并群打破这个前提：节点并入超级节点后，边的可见性在两个方向上**不对称**——反向边可能只以群粒度存在、或者根本没有被显式建出来。gP ≠ reverse(gS)。
- 后果：双向搜索会**漏路径**——返回"不可达"而单向搜索返回"可达"。这不是"更快地算出同一个答案"，是**算出不同的答案**：调度构建的合并决策直接被改变，输出不再恒等。

判定与处置（每一步都可迁移）：

1. **对派生/quotient 结构做算法替换前，先证它需要的不变量**——而且要在**经历全部变更（合并）之后的 live 结构**上证，不是在原始图上证。原始图上 gP = reverse(gS) 成立不代表合并后成立。
2. **用差分 harness 证伪**（§4.3）：把纯决策函数抽出来，旧新实现在同输入上对拍，合并群/不对称边这类对抗性输入一打，答案分叉立现。
3. **带证据回滚，先于 commit**：探针被测量、被证伪、被回退，证据（哪些输入上分叉、为什么）记录在案——与 [[wiki-ab-isolation-discipline]] §2 的撤回链同构，只是这次撤回发生在**测量之前**的 harness 阶段，成本便宜两个数量级。**会改变答案的"加速"不是加速**，永远不上 commit。

### 3.4 处置总表

| 改动 | 类型 | 输出恒等？ | 依据 | 处置 |
|---|---|---|---|---|
| canon 哈希按记录并行 | 并行化 order-free 函数 | 是 | 构造上成立（§2） | 落地 |
| `std::set` → 排序平铺向量 | 数据结构替换 | 是 | 内容键排序（§3.1）+ §4 门验证 | 落地 |
| 可达性按合并代备忘录化 | 缓存 | 是 | 缓存键 = 图代，同代图不变（§3.2） | 落地 |
| 双向搜索替换单向搜索 | 算法替换 | **否** | gP ≠ reverse(gS)，漏路径（§3.3） | harness 证伪 → 回滚 → 记录 |

---

## 4. 生成器改动的三重确定性门禁

生成器速度改动与 [[wiki-ab-isolation-discipline]] §1.1 的 flag-off 恒等有一个关键差异：**那里每个新特征有默认关闭态可回退，这里速度优化没有"关"**——加速必须无条件保持输出恒等，否则等于永久改写了基线。因此门禁是无条件的，且三道各司其职：

### 4.1 门一：字节恒等模型

改动前后各做一次完整配方生成，对**全部生成源文件**逐字节 diff（外加小设计集与分片并行构建）。这是端到端的最终判据：任何一道下游门漏掉的漂移都会在这里暴露。局限：粗粒度——分叉了只能告诉你**有**漂移，定位要靠门二。

### 4.2 门二：种子回放 canon 点

用既有种子回放（[[wiki-pointer-order-nondeterminism]] §5.2/§9 的逐点强制 + canon 校验链），在**钉住的中间点**上比对 canon 哈希。它把"生成器被我改坏了吗"从端点问题变成逐 pass 问题——第一个分叉点直接指名肇事 pass。种子必须与改动后的生成器**重新录制对拍**（旧种子只在旧二进制上有效）。

### 4.3 门三：纯决策函数差分 harness

把调度构建的决策逻辑抽成纯函数 harness（给定图状态，输出合并决策/可达性答案），旧新实现在相同输入序列上对拍：

- **成本结构逆转**：harness 跑秒级，完整生成跑分钟到小时级。它把"改坏了没有"从昂贵的端到端问题变成便宜的单元问题——§3.3 的双向搜索正是在这里被证伪，而没有消耗任何一次完整生成。
- **对抗性输入是 harness 的价值所在**：模型语料未必覆盖合并群/不对称边这类边界；harness 可以直接构造。语料能过的差分不等于 sound，专门设计的边界输入才能翻出 gP ≠ reverse(gS) 这类结构缺陷。
- **它同时是优化的证据链**：harness 上记录的缓存命中率/答案一致数，就是 §3.2 备忘录化收益的直接度量。

### 4.4 三道门的分工

| 门 | 覆盖 | 粒度 | 成本 |
|---|---|---|---|
| 字节恒等模型 | 端到端全部输出 | 有/无漂移 | 两次完整生成 |
| 种子回放 canon 点 | 逐 pass 中间状态 | 哪个 pass 分叉 | 一次生成 + 回放 |
| 决策函数差分 | 被改算法本身 | 哪类输入分叉 | 秒级 |

三道门缺一不可：只有门一，定位靠盲猜；只有门二，算法层的结构缺陷要等语料撞上；只有门三，集成层（发射、序列化）的漂移无人看守。

---

## 5. 检查清单

1. 优化讨论开始前，每个顶层 pass 一对时间戳；未插桩区域按"未知"处理，不贴标签。
2. 调试/dump 路径审计重复构建；dump 只读既有结构或被开关彻底门控。
3. "格式化/发射类"假设先拿数字（案例：发射占被指认阶段的 1.6%）再排期。
4. 只并行化**按定义 order-free** 的函数；"单线程下恰好确定"不是并行化许可。
5. 并行哈希的逐记录字段排除表与既有 canon 二分器逐项一致；一次改动只动并行化，不动字段语义。
6. `std::set` → 排序平铺向量时排序键必须是内容键；性能动机与确定性修复同时写进提交说明。
7. 缓存必须带显式失效键（合并代/图版本），且键枚举结果依赖的全部输入。
8. 对派生/quotient 结构替换算法前，先在**经历全部变更的 live 结构**上证明算法需要的不变量（gP = reverse(gS) 类）。
9. 证伪的捷径：harness 对拍证 unsound → 回滚先于 commit → 证据入库；会改变答案的加速不是加速。
10. 每个生成器速度改动交付三道门：字节恒等模型 diff + 种子回放 canon 点 + 纯决策函数差分 harness。

---

## 相关页面

- [[wiki-value-change-census]] - 变值普查："census before emit"的同型纪律用在运行时生成代码上；本文把它用在生成器自身
- [[wiki-ab-isolation-discipline]] - A/B 隔离纪律：flag-off 字节恒等与撤回链；生成器速度改动把恒等要求升级为无条件（没有关闭态）
- [[wiki-pointer-order-nondeterminism]] - 指针序非确定性：canon 门、种子回放、规范化指纹二分的机制来源（§4–§5、§9）；§3.1 的排序平铺向量同时是其 §2 病灶的修复
- [[wiki-reproducibility-and-config]] - 可复现性与配置：种子回放与配方逐字一致的约定
- [[wiki-scheduling]] - 调度：运行时调度与负载均衡（与本文的生成期调度构建相对）

## 参考来源

- 案例项目 gsim-mt（多线程 RTL 仿真器原型）：本文全部实测数据来源（阶段计时、canon 哈希并行化、合并循环数据结构与备忘录化、双向搜索证伪与回滚、三重门禁记录见项目任务报告）


---

## 追记（2026-08-27）：陈旧表项 = 活边副本——假性环拒绝

合并循环的原始邻接表（gS/gP）里，被吸收群组的残留表项在 find() 规范化后成为**活边的重复副本**。候选评估协议是"临时擦除直连边再查 pathExists"——擦除按值删**一个**出现，死副本让直连边对 DFS 仍然可见 → pathExists 平凡命中 → 合并被**误判为成环**而拒绝。

T32 冠军图实测：cycRej **3,286,710 → 200,279（16×）**，merges 32,887 → 36,909，MTasks 12,275 → 8,253。更粗的调度反而**更快**：C50000 五对 5.008s vs 5.668s（-11.6%，区间不重叠），Linux 100K -8.4%。注册为新 T32 冠军（xiangshan-t32-compact-v1）。

三条可迁移判定：

1. **"中性"内部优化假设可能在信息量大的方向上错**——本例假设死表项压实只是提速，manifest 恒等门在性能声明**之前**抓到了调度变化，把一个静默的启发式缺陷转成 11.6% 的实测收益。恒等门不只是安全网，也是发现器。
2. **并查集 + 临时擦除的语义交互**要显式审计：擦除-恢复协议下，等价类代表元的重复计数不是无害冗余，它改变可达性查询的答案。
3. **更少的选择有时是更好的选择**：16× 的假拒绝一直在压制合并启发式；"拒绝率高 = 图难合并"的直觉可能是数据结构自伤的伪影。

配套修复纪律：门控默认关（注册冠军的调度建立在旧行为上）；配方 = 既有种子（钉点到 preEmit）+ 压实旋钮，vcontract 在钉点后自由运行——新调度是 (种子钉, 压实) 的确定函数，逐次运行字节一致；无种子的自由写在 jemalloc 下落在已知 splitArray 平局边界上（sccs 45161 vs 45162），配方必须走种子路径。


### 追记补：逐档位结局（2026-08-29 修正：两轮 T16 数据均为跨图污染）

**原始结论已被推翻。** T16 压实 +1.5% 与 +7.2% 两次测量均为跨分配器/跨图对比（jemalloc 二进制 vs glibc 图的冠军）。确定性修复后同图 A/B（同一二进制、sccs=45163 双侧一致）**翻转结论**：T16 压实 **-8.4% 更快**（{6.43-6.47} vs {6.94-7.13}，5 对不重叠）。

**修正后的教训**（比原教训更深一层）：在证伪模型之前，先验证证伪所依据的测量——跨图污染不仅使结论无效，也使"证伪"本身无效。本例中两项分数模型两次都选了 COMPACT（对的），我的"证伪加固"却建立在坏数据上。正确的等级是：测量有效性 > 模型验证 > 模型证伪。POLICY=auto 已回退纯分数规则。


### 追记补 2：输入自适应策略选择（2026-08-28）

用户点破的方向：**优化策略的选择本身应当是输入的函数**——理解每个优化的本质（它交换什么、付出什么），在任意 RTL/workload 输入下自动选出对症的那个。

压实旋钮的本质结构：
- **换来**：跨线程依赖边 -40~55%（同步点减少）
- **付出**：任务粒度变粗（均衡打包变难、fill/drain 变长）
- T32（同步开销绑定）→ 赢 11.6%；T16（工作地板绑定）→ 输 1.5%（合并后 7.2%）

`POLICY=auto` 的实现：代码生成期**双 pass 构建两个候选调度**（契约构建器自包含，第二遍从原始商图重启），用两项地板模型（真实列表调度器的 maxW + 边数 × 实测 token 延迟）评分选优。

**两次证伪的教训**（这是比成功更值钱的部分）：
1. 自由 LPT 评分分配器把 maxW 算成完美均衡——恰好抹掉了决定 T16 的那一项；
2. 换上真实依赖感知分配器后，模型仍选 COMPACT——因为缺失的 fill/drain 与粒度项**都反压实**，缺失项模型必然朝它缺失的方向错。
最终规则：**（线程数先验 ∧ 模型同意）的合取**。校准过的先验承载数据，模型只做高线程否决。

**方法论沉淀**：三点标定两常数的地板模型是假设不是策略；合取（校准先验 AND 模型）是缺失项未建模前的诚实折中。策略选择器的每一次升级都应来自一次新的证伪。


### 追记补 3：调优冠军经确定性修复完全恢复（2026-08-29）

确定性修复（splitArray/checkNodes/mergeNodes 名序化）改变了图构造（sccs 45162→45163），使新鲜搜索的调度质量回退（T32 +12%）。但**种子钉点从 pass.topoSort 开始——在全部被修复的图构造 pass 之后**。种子直接钉住调度决策，绕过图构造的变化：compact-v2 种子在修复后 tip 上 replay canon 9/9 全过、模型字节一致、性能完全恢复（{4.84, 4.83, 4.82} = 原冠军 4.83s）。

**架构级洞察**：seed2 机制天然将确定性范围限定在调度决策（图构造之后），因此图构造的确定性修复不会使调优种子失效。这与"确定性 vs 质量"的紧张关系形成对照——决策回放不仅零税，而且对上游代码变化天然鲁棒。新鲜搜索的 MAXMT 扫描（1800 最优）仍对无种子生成有价值。

T16 恢复（v457 种子）是类似跟进。


### 追记补 4：新 RTL（kunminghu-v3）适配的 splitArray 动态索引问题（2026-08-29）

上游 kunminghu-v3（253 提交）引入 FTQ `curPerfMeta` 结构（120 处引用，旧 RTL 为 0），其中 `cfiAttr[newBranchInfo.cfiPosition]` 使用**运行时计算的索引**——`cfiPosition = bits(add(io.fromBackend.resolve[1].bits.ftqOffset, T_3), 4, 0)`。gsim 的 `getIdx()` 只解析常量索引（`OP_INDEX_INT`），对动态表达式返回 (-1,-1)，`distributeTree` 断言失败。**上游 gsim 同样未修**（同断言存在，`hasVarIdx` 定义但从未用作守卫）——这是 gsim 图模型对动态索引寄存器数组的架构级缺口。

**三次尝试的教训**：
1. 跳过不拆（skip）：数组留在 partialVisited，拓扑排序断言——splitArray 本身是 Kahn 拓扑排序的一部分，数组拆分用来打断依赖环，不拆 = 环未断
2. 标记已访问绕过（mark-visited）：环上的组合逻辑链（FTQ _T_149→150→151）卡死——自环仍在
3. mux 展开（advisory 建议）：`member[i] = mux(idx==i, rhs, member[i])`——正确方向但 false 分支引用 member[i] 产生自环

**根因**：寄存器数组的 `connect arr[idx], val` 是时钟沿语义（`arr[idx]$NEXT = val`），mux 展开必须走寄存器的 $NEXT 机制（如同静态索引写），不能组合自赋值。修复需要理解 splitArrayNode 对静态索引寄存器写如何挂 resetTree/$NEXT，然后把动态索引树路由到相同机制。

**资产已保全**：mux-expansion 半成品在 `stash@{0}`（/tmp/gsim-wip-b1-lookahead），新 FIR 在 build-sv/rtl/（1.46GB），旧 v86 冻结在 rtl-v86-frozen/。工作树已恢复干净（FIR 门 22/22）。


### 追记补 5：新 RTL（kunminghu-v3）适配完成——when-展开方案（2026-08-30）

**最终方案**：`distributeTree` 对 (-1,-1) 动态索引写做**when-展开**。到达 distributeTree 的树是 `arr[dynIdx] = when(cond, rhs, prev)`（AST2Graph 的 last-connect 机制保证两分支齐全），对每个成员 i 发射 `arr[i] = when(dynIdx == i, 原树, EMPTY)`：lvalue 保持数组引用+常量索引 i，`updateWithSplittedArray` 后续改写为成员引用；动态**读**本来就有支持（updateWithSplittedArray 的 mux 链分支）。提交 770bab7。

**之前三次失败的教训**（见追记补 4）：跳过/标记已访/自引用 mux 都不行——分别破坏拓扑排序、留环、造自环。when-展开复用全部现有机制（when 合并、静态分支改写），是唯一与图算法相容的表达。

**配套修复**：新 RTL 的 DPI extmodule（TopdownIQInfoHelper_80/238、TopdownRobInfoHelper_318_352）需要在 difftest-extmodule.cpp 手写 wrapper（gsim 只发声明，定义来自 difftest 侧）。参数映射已验证无缓冲区溢出。

**验证**：727 cpp / sccs=65965（新 RTL 比 v86 大 46%）→ 编译 → CoreMark 2-iteration **HIT GOOD TRAP**（663758 指令 / 304246 周期 / IPC 2.18），NEMU difftest 全程零失配。旧 RTL FIR 门 22/22 不受影响。

**新 RTL 特性**：~12k 周期慢启动（C8000 仅 6 指令，C16000 起 IPC 2.40）——kunminghu-v3 的 boot 特性，非卡死。旧 C5000/C50000 门是周期检查点，不跨 boot 时间变化迁移；新参考点 = 完整工作负载 663758 指令、trap pc 0x80001ca0。


### 追记补 6：新 RTL 冠军注册 + 种子合同（2026-08-30）

新 RTL（kunminghu-v3）两个档位冠军注册，均带 write==replay 验证的种子：

| 冠军 | linux-30k | mtasks | 种子验证 |
|---|---|---|---|
| newrtl-t32-compact-v1 | **7.01s** | 10463 | 9/9 钉点，哈希一致 |
| newrtl-t16-compact-v1 | **8.21s** | 6258 | 9/9 钉点，哈希一致 |

（正确性合同：coremark 全量 difftest HIT GOOD TRAP 663,758 指令零失配，四个配置位一致。）

COMPACT 在新 RTL 上 -5.1%（T16 交错 3 对），与旧 RTL 的 -8.4% 方向一致。

**坑**：种子写运行若 `--dir/model` 目录不存在，会在最终 dumpMtCoarseRegionReport 崩溃（cppEmitter 会建目录但这个报告 fopen 不会）——生成前必须 `mkdir -p <dir>/model`。种子写耗时约为普通生成 2.2×（2095s vs 937s，含决策记录开销）。

**下一个杠杆**：两个新冠军都是无调优新鲜调度——旧 RTL 从种子调优获得 ~10%，新 RTL 同一机制可用。


### 追记补 7：新 RTL 单线程 + Verilator 对比完成（2026-08-30）

Linux 30k 周期、无 difftest、线程宽度匹配、CPU 隔离串行测量（README 提交 bafb638 + 42731c2）：

| 线程 | gsim | Verilator | 加速比 |
|---|---|---|---|
| 1 | 37.7s（纯串行模型，367 cpp）| 269s | **7.1×** |
| 16 | 8.21s（T16c 冠军）| 15.4-15.9s | **1.9×** |
| 32 | 7.01s（T32c 冠军）| **构建不出**（UNOPTTHREADS）| 2.2× vs V-T16 |

**关键发现：Verilator 5.034 无法为此网表做 32 线程划分**（75 分钟 verilation 后 UNOPTTHREADS 报错退出）——新 RTL 的组合结构触及 Verilator 线程调度极限，而 gsim-mt 的 SCC/MTask 划分正常处理 T32。这是网表结构层面的发现（老 RTL 的 V-T32 能建，9.73-9.84s）。

**跨仿真器功能交叉验证**：Verilator T1/T16 与 gsim T1/T16c/T32c 五个独立生成的仿真器在同一窗口全部提交精确一致的 86,469 条指令。

**Verilator 构建坑**（记录备查）：(1) 独立 difftest_verilog 的输出绝不可写进集成构建目录（endpoint step 端口不匹配破坏 Verilator，这也是 FIR 事故的诱因）；(2) generated-src 必须来自集成 elaboration（带后缀的 DPI wrapper 只在那里生成）；(3) EMU_THREADS 宏改变全部编译 flags → 跨线程数构建 ccache 全 miss；(4) 串行 gsim 生成必须 env -i 清掉 gen-env.sh 残留（GSIM_MT_DENSE_EXECUTOR_CODEGEN 会让串行发射断言 useCoarseMt）。

串行基线已注册 champions/newrtl-serial-baseline（含 6 次运行 + HIT GOOD TRAP 门）。


### 追记补 8：2× 同线程 Verilator 达成——MAXMT 随图规模重整（2026-08-30）

**目标**：新 RTL 同线程达到 Verilator 2×。**达成**：gsim T16 MAXMT=1700 = **7.19s** vs V-T16 15.35s = **2.13×**（保守配对 2.10×），正确性门 HIT GOOD TRAP 位一致，种子 write==replay 字节一致（`1380be88`）。注册 `newrtl-t16-compact-v2`，README `21392ef`。

**方法**：MAXMT=800 是从 v86（45,163 sccs）抄来的配方；kunminghu-v3 大 46%（65,965 sccs），交错扫描 {800,1100,1400,1700,2000,2400}（热身丢弃、固定掩码、串行机器）：

| MAXMT | 800 | 1100 | 1400 | 1700 | 2000 | 2400 |
|---|---|---|---|---|---|---|
| 均值 s | 8.17 | 8.14 | 7.57 | **7.25** | 7.20 | 7.62 |

曲线在 1700-2000 见底（5 对决胜 7.192 vs 7.198 平手），2400 回升——最优点随图规模上移约 2 倍，与"任务预算应随 SCC 数缩放"的直觉一致。

**流程规则**：跨 RTL 迁移配方时 MAXMT 必须重扫（图规模变了最优点就变）；本例 +11.4% 性能全来自这一个参数。缓存控制：ccache 只影响构建墙钟（key 含预处理源码，不同模型必 miss，运行时不受影响）；bench 前热身丢弃（页缓存）；交错轮次控频率漂移。


### 追记补 9：MAXMT 预测系统——层级同步不变量（2026-08-30，提交 114fec0）

**本质**：MAXMT = 调度粒度，在"层级负载均衡收益"（随任务数递减）与"跨线程同步开销"（随任务数递增）之间取边际交换点。执行器是层级同步的——周期时间下界是 `lvlSum = Σ_层 max_w(该层该 worker 的活)`，不是全局 maxW。这个物理量在发射前就能从调度算出。

**系统**：`GSIM_MT_DENSE_VCONTRACT_MAXMT_AUTO=1` 在生成内重跑收缩（pristine 梯度副本机制），按 lvlSum 排序选优。成本 +4-5 分钟（khv3）/+2 分钟（v86），零构建。

**双 RTL 验证**：
- v86-T16：lvlSum argmin = 1200 = **实测最优精确命中**（5.51s；默认 800 是 6.22s——**默认值差 11.4%**）
- khv3-T16：选 1600，被实测 1400(7.57)/1700(7.25) 夹住，距最优 2000(7.20) 约 1-3%

**失败的模型（保留教训）**：maxW+cross 地板两项在两个 RTL 上都排错；三项墙时拟合不跨图泛化（LORO 双向失败）——全局 maxW 缺层级结构这一物理量。已知局限：有界 lookahead 会拯救部分被层级地板高估的调度（khv3 1700 实测最好但 lvlSum 偏高）——只会高估不会低估。

**未见 RTL 的推荐协议**：探针排序 → 取 top-2 → 构建+交错实测确认。总成本 = 1 次生成 + 2 次构建，对比全扫描 6-7 个点。


### 追记补 10：humanize2 集成战役终态——2.5× 的完整证伪链与 CCD 局部性方向（2026-09-01）

12 轮 humanize2 驱动（builder glm-5.3:max + reviewer codexa/gpt-5.6-sol）后的完整结论：

**2.5× @ khv3-T16 测量证伪，无测量缺口**：旋钮（3 会话，最优 -2.58%）｜同步搁浅（3-6% 天花板，编入计数器实测）｜占空比（95%+ 饱和）｜运行时跳工（全负）｜**发射减指令（-25.4% 指令 → 墙钟 +0.55%，解耦证明）**｜布局-尾侧（1.6% 周期）｜**perf 归因分解**（backend-stall 23.2% 周期中 79.9% 在 mtask 体的强制性流式访问：81.2MB 共享 RTL 状态 vs 64MB 双 CCD L3——工作集物理不匹配）。

**关键方法论教训**：
1. "miss 份额 ≥20% ⇒ 布局杠杆活" 的决策规则太粗——必须分解停顿质量的归属（体 vs 尾 vs libc），否则会攻击只带 1.6% 质量的目标
2. 指令数与墙钟解耦是"延迟受限"的必要非充分证据——归因分解才是终局证据
3. 三次独立 STOP（high/max 档不同模型）递进收敛，最终一个带逐分量归因数字

**唯一归因支持的后续项目**：CCD 局部性重分区——分配 mtask 使每 CCD worker 集触碰不相交的状态半区，≤18.5% 周期上限；属分区器架构项目（目标函数+发射），非轮尺度杠杆。

**基建遗产**：pi 垫片（omp 即 pi）、台账信号回合完成、reviewer-aware watcher、7 个根因修复——`scripts/gsim-humanize.sh` 随时可重启。


### 追记补 11：CCD 局部性指派——归因驱动杠杆落地（2026-09-01，提交 ddf4574）

**humanize2 归因轮的预言兑现**：t16-memstall-attribution 指出的 CCD-locality 方向（≤18.5% 上限）以软指派项形式落地——`GSIM_MT_DENSE_PACK_CCD_AFFINITY=<pct>`（默认关）：贪心表调度器对跨 8 核 L3 边界的生产者→消费者 mtask 对加附加惩罚。拓扑用 lscpu 实证（T16 = CCD0 0-7 + CCD1 8-15，解决归因条目与旧分数模型的拓扑矛盾——旧模型 L=1 是校准错误）。

**注册 newrtl-t16-compact-v3**（γ250 × MAXMT2000 × LA512）：5 对交错 5/5 全负、区间分离 [7.00-7.07] vs [7.38-7.42] = **-4.76%**，HIT GOOD TRAP 位一致。γ 曲线 250 饱和（150→7.23, 250→7.05, 400→7.46, 600→7.33；细调 200/300 均略差）。**同会话比率 2.32×**（V-T16 16.36 / gsim 7.06）。

**实现陷阱（重要）**：第一版把惩罚加在 PackThreads 指派器——但配方走 SCHED_ORDER 路径，旋钮静默无效，模型与冠军字节一致。**规则：任何指派/调度旋钮改动的验证必须包含模型哈希对比**（若与基线一致 = 旋钮没接上）。

**状态**：2.5× 未达（差 7.2%，需 ≤6.54s）；t16-25x-terminal-final 的"不可达"结论被部分推翻——归因支持的杠杆确实落地了一个。下一级杠杆是分区器级状态-CCD 重分区（把 81MB 状态按 CCD 划分所有权），仍是项目级工作。


### 追记补 12：B1-B3 执行与 harness 代差规则（2026-09-01）

**B1（v86-T16 重注册）**：MAXMT=1200 注册为 xiangshan-t16-compact-v2，同 harness 5 对 **-12.1%**（5/5 全负区间分离 [5.44-5.50] vs [6.09-6.30]）。种子 write==replay 字节验证。

**harness 代差规则（重要协议）**：advisory 红灯（ic 46,562 vs 46,540）的根因——difftest 子模块更新（45638f514）前构建的冠军 emu 与当前 harness 构建是**不同系统**（不同 boot 流程/陷阱 pc/周期计数），各自对各自的 NEMU 门正确，但**跨 harness 对比无效**。规则：旧冠军 emu 参与任何对比前必须在当前 harness 上重建基线。

**B2（T32 扫描）**：lvlSum 探针选 m4800 + CCD250，linux-100k vs 冠军 m2400 = -0.4%（噪声）——T32 档平台期；T32 24.2s ≈ T16 24.8s，档位扩展在 100k 负载饱和。

**B3 汇总（README 双表 + workload 刷新）**：linux-100k T16 2.18×/T8 2.07×，CoreMark C50000 2.27×，T1 7.1×。Arcilator（D3）按用户指示跳过：上游 HEAD（本机构建 llvm/circt）对香山的 seq.firreg async reset 不支持——与 GSIM 论文记载一致；FIR→MLIR 的 XMR/bind 障碍已解（--verification-flavor=none --default-layer-specialization=disable），构建配方留存 /tmp/circt。


### 追记补 13：B4 状态-所有者布局——CCD 项目最小切片（2026-09-01，提交 c7ce312）

`GSIM_EMIT_STATE_BY_OWNER=1`（默认关）：把状态成员声明按其所有者 mtask 的 worker 所在 CCD 重排（稳定索引排序，45281/66120 super 归因），每 CCD 的写入状态聚成连续 arena——纯布局、语义零变、FIR 门 22/22。

**结果**：5 对 vs v3 冠军 **-1.28%**（5/5 全负、区间分离），门位一致——真实但低于 3% 注册线，记录不注册。与本会话 CCD 亲和指派项（-4.76%）叠加：T16 会话进程 7.19s → 6.93s 观测最佳。

**教训**：(1) gcc/clang 对 SuperNode* 的 stable_sort 触发 deprecated temporary-buffer Werror——用索引排序+原序决胜等价替代；(2) MtDenseScc 携带 cppIds（非成员节点）——scc→supernode→node 的映射链要走 supersrc。

深层重分区（任务级发射分区+跨 CCD 边特化）理论余量 ~13%，留作后续项目。


### 追记补 14：旋钮面终局与会话最终态（2026-09-01）

SBO×γ 交互扫描关闭最后一个旋钮组合（γ250 无 SO 仍最优）。

**会话最终态（全部轮尺度杠杆类证据关闭）**：
- T16 linux-30k 最佳观测 **6.93s = 2.36×** 同会话（目标 2.5× = 6.54s，差 5.6%）
- T16 linux-100k 2.18×；CoreMark C50000 2.27×；T8 100k 2.07×；T1 7.1×
- v86-T16 冠军 v2（-12.1%）；T32 平台期
- 关闭的杠杆类：旋钮×3 会话、同步天花板 3-6%、占空比 95%+、跳工全负、减指令解耦（-25.4%→平）、尾布局 1.6%、CCD 软指派（-4.76% 已注册）、状态-所有者布局（-1.28%）、SBO×γ 交互

**唯一剩余方向**：深度每 CCD 状态/任务重分区（~13% 理论余量）——多日项目级，需立项授权。


### 追记补 15：KL 全局割最小化负结果——CCD 方向的会话终局（2026-09-01，提交 e385d85）

`GSIM_MT_DENSE_CCD_KL`（默认关）：增量增益 KL 对交换（incident-edge 局部 Δ、候选上限 128、a-b 边自消、per-worker 负载追踪）。advisory 在首次实现烧机前捕获复杂度炸弹（O(n²)×O(E)≈10¹²——cutCost 全图重算塞进对循环），增量版 ~10⁷ 完成。

**负结果**：6706 交换把跨 CCD 边割砍到 32.5%，但墙钟 **+3.5% 变差**（均衡损失主导）。正确性无恙（门位一致）——纯指派实验的干净否定。

**结论**：贪心均衡优先的 CCD_AFFINITY（-4.76%，已注册 v3）确认在割/均衡权衡的甜点附近。CCD 方向会话级菜单穷尽：贪心 ✅、布局 ✅（-1.28%）、全局割 ❌（+3.5%）。剩余深化（任务级发射分区+跨 CCD 边特化的真重分区）仍为多日项目。


### 追记补 16：CCD 方向四点闭环——静态割非绑定约束（2026-09-01，提交 5439f83）

严格均衡 KL（只换 25% 成本相近的对）保住负载同时把割降至 41.1%，墙钟**平手**。与宽松版（割 32.5% 但 +3.5%）合并解读：**静态跨 CCD 边割不是绑定约束**——γ 贪心的 -4.76% 来自**时序共置**（表调度进行中的延迟感知排序），不是静态割结构。

CCD 方向证据四点闭环：
| 变体 | 割 | 墙钟 |
|---|---|---|
| 贪心 γ250（均衡优先）| 基线 | **-4.76%** ✅ |
| SBO 布局 | — | -1.28% ✅ |
| 宽松 KL | 32.5% | +3.5% ❌ |
| 严格 KL | 41.1% | 0.0% ➖ |

**方法论教训**：优化"看起来对"的代理目标（静态割）前，先证明它与会话目标（墙钟）因果耦合——本例四点证据表明两者解耦。真深度重分区若立项，目标函数必须含时序维度。


### 追记补 17：WP2′ 量化、kill-test 与手术重定界（2026-09-01 会话终）

**量化**：1.11M 静态 `$old$` 拷贝，53.3% 死（592K，文件级保守计数后 386K）。Kill-test（文本剥离 + 编译器 oracle）：**-0.9% 区间重叠**——死拷贝早被 LLVM DCE。

**归因修订（解析回答）**：61.7G 指令超额全部在 **136G 扫描+派发池**内（36.6% × 372.2G）；原归因正确，死拷贝子假设错误。

**手术 (b) 重定界为融合比较-扫描**（`bb2d4b9` 实现草案）：
- 排序每任务等待表（按 token 索引 → 64 路缓存行局部性）
- 64-token 窗口批量检查（一次 64B 加载替代最多 64 次独立加载）
- 预期 15.1 → 3-5 有效加载/条目，天花板 ~10% 墙钟 > 5.6% 缺口

**会话 30.8 小时总结**：集成三标准 ✅，2.13×→2.38×，22+ 假设全测量（含 8 否定），158 条台账，6 冠军种子封存。唯一剩余手术已完整文档化（目标函数、kill-test 结果、实现草案、发射位点、验证链）——新会话授权后即刻开工。


### 追记补 18：扫描方向五行闭环与会话最终数字（2026-09-01）

扫描杠杆五行实验（sorted / consecutive-batch / word-batch / bank-reorder / true-arena）：
**sorted-waits 的 -1.53% 就是扫描局部性的全部可得收益**。扫描每候选成本由间接索引主导（waitList[wait]→tokenIdx），布局无法消除。分配器真重写（`1e58e81`，消灭 pair-bank、consumer token 连续）验证了这一结论。

**会话最终数字**（34 小时，166 条台账，~30 假设）：
- T16 linux-30k 全栈（CCD250+SBO+sorted-waits）：**6.93s = 2.30×** 同会话 vs V-T16 15.91s
- v86-T16 冠军 v2：**-12.1%**（MAXMT=1200 重注册，种子封存）
- 指令超额（61.7G）完全在**活比较位点**（520K USED `$old$` 比较）——批量激活手术（WP2′）为唯一剩余路径，前置归因已完成、kill-test 已执行（死拷贝否定）、设计草案已写

**方法论遗产**：MAXMT lvlSum 预测（双 RTL 验证）、harness 代差规则、静态割解耦四点证明、代理目标证伪方法论、线程宽度匹配规则、验证后改码陷阱、编译器 oracle kill-test 模式。


### 追记补 19：WP2′ 三切片闭环——事件检测的内在代价（2026-09-02）

WP2′（批量激活）最终归因，三个切片全部实测：

1. **死拷贝剔除**：386K 静态死 `$old$` 已被 LLVM DCE——动态零效果
2. **OR 位点批量**（`aebd258`，bacc[256] 累加器，447K→39K 静态压缩 11.5×）：**+1.16% 回归 5/5**——OR 从来不是成本；LLVM 本就合并同地址 OR 链
3. **SIMD 打包 diff**（投影上限分析）：nvec 加载（+N）抵消 SIMD 比较节省（-0.75N）；加载主导比较成本；上限 ~1-3%

**结构性结论**：525K 活比较（`X != X$old$`）就是事件检测机制本身。Verilator 付出零（全静态求值）；gsim 付出比较以跳过工作。**9% 差距 = 事件驱动架构在该粒度的内在代价**。突破需要更粗的激活粒度（层级汇总位）——激活协议重设计，非发射微调。

正确性副产物：OR 批量语义已证明位一致（CoreMark 门通过）——未来若激活粒度改变，此不变式可复用。


### 追记补 20：LA1024 发现与 v4 注册——全栈交互教训（2026-09-02）

**LA 曲线未在 512 饱和**：早期扫描（512 最优）的停止规则"段长 ~530"是在 CCD 亲和之前测的。全栈下 {512, 768, 1024, 2048} = {7.050, 6.990, **6.910**, 6.940}——**最优在 1024**，v4 注册（种子 `2348c8900e5d3ce8`，-1.98% 5/5）。

**超节点粒度关闭**：sz {20,30,60} = {7.25, 7.06, 7.68}——30 最优；+60 损失 3.4× 于 -10，跳过粒度敏感性主导（事件驱动价值的独立印证）。

**harness 代差规则实战**：build-vrtl 的 V emu 是七月旧 harness（instrCnt 65,889，11.5s）vs 当前（86,469，15.3s）——30% 差距完全来自 boot flow。任何 V 比较前必须先验 instrCnt 身份。

**方法论教训（本轮最重要）**：每个栈变更（如 CCD）后，承重参数（LA/MAXMT/γ）的最优可能移动——必须重扫，不能沿用变更前的停止规则。

**V-T16 分母漂移**：同 harness 跨会话 15.3-16.4s（±7% 机器状态漂移）——比率报告必须注明当日分母。


### 追记补 21：跨 RTL 迁移图谱——v86 v3 注册（2026-09-02）

**迁移测试完备**：kunminghu 会话的旋钮在 v86-T16 上逐一验证：

| 旋钮 | 类 | 迁移？ |
|---|---|---|
| CCD 亲和 γ250 | 发射/布局 | ✅（复合 -3.43%）|
| SBO 状态布局 | 发射 | ✅ |
| sorted-waits | 发射 | ✅ |
| LOOKAHEAD 1024 | 调度 | ❌（v86 在 128 饱和）|
| MAXMT 2000 | 结构 | ❌（v86=1200）|

**v86 v3 注册**（`xiangshan-t16-compact-v3`，种子 `d3bb2feaab58052b`）：-3.43% 4/5，coremark 零失配。

**规则**：发射/布局层旋钮可跨设计迁移；调度/结构层旋钮是设计特有——先迁移发射层，再按设计重扫调度层。


### 追记补 22：γ 迁移精化——机制可迁移，最优值设计特有（2026-09-02）

v86 γ 全曲线 {150, 250, 350, 500, 600} = {3.147, 3.223, **3.105**, 3.365, 3.305}——干净凸形，最优 **350**（kunminghu 是 250）。**v86 v4 注册**（种子 `5e580a7636dd334b`，当日累计 **-8.1%** vs 裸栈）。

**迁移规则精化**：γ 作为机制可迁移，但其最优值设计特有（更小的设计想要更强的 CCD 惩罚）。迁移后必须重扫——本纪律在 v86 上兑现了 -3.7%（朴素迁移会留在桌上）。
